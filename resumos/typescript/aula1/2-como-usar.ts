/*
    Como instalar:
        npm install -g typescript
        npm install -g tsnode
        typescript -init -> gerar tsconfig.json

    Como compilar:
        tsc {path}      -> transpilar arquivo ts para js
        tsc -p tsconfig.json --noEmit 
            -p          -> caminho para o tsconfig.json do projeto
            --noEmit    -> não criar o arquivo js
            
        ts-node {pathToFile}


    Compilar x transpilar:
        Mais sobre o objetivo em si do que o que acontece.
        Transpilar é um tipo de compilação, que o resultado ainda é uma linguagem de alto nível
        Compilar é com o resultado pra uma linguagem do nível do computador


    
*/