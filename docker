docker container prune - remove todos os containers que estão parados.

docker inspect ID_CONTAINER - retorna diversas informações sobre o container.

docker network create  - cria uma rede.
	--driver bridge NOME_DA_REDE - 


docker ps - exibe todos os containers em execução no momento.
   -a - exibe todos os containers, independente de estarem em execução ou não.

docker run NOME_DA_IMAGEM 	- cria um container com a respectiva imagem passada como parâmetro.
   -e AUTHOR="Fulano" 		- define uma variável de ambiente AUTHOR com o valor Fulano no container criado.
   -it 						- conecta o terminal que estamos utilizando com o do container.
   --name NOME 				- ao executar, dá um nome ao container.
   --network NOME_DA_REDE   - especifica qual rede deverá ser usada.
   -p 12345:80 				- define uma porta específica para ser atribuída à porta 80 do container, neste caso 12345.
   -v "CAMINHO_VOLUME" NOME_DA_IMAGEM - cria um volume no respectivo caminho do container.

docker rm ID_CONTAINER - remove o container com id em questão.

docker rmi NOME_DA_IMAGEM - remove a imagem passada como parâmetro.


docker stop ID_CONTAINER - interrompe o container com id em questão.
docker start ID_CONTAINER - inicia o container com id em questão.

docker version - exibe a versão do docker.
